# AHFMES ARE — Self-Audit Council Protocol V17

Status: **MANDATORY PRE-EXTERNAL-REAUDIT GOVERNANCE / SUBJECT-BOUND QUARANTINE ROUTING / STATELESS / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-22**

## 0. Composition

Immutable base:

```text
BASE_PROTOCOL_V16_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_SELF_AUDIT_COUNCIL_PROTOCOL_V16.md
BASE_PROTOCOL_V16_GIT_BLOB_SHA = afdb1b5d019b177085b099390b2a3a47e3e4fe90
```

All V16->V2 rules remain except current manifest generation, quarantine-policy routing, historical impact disposition and regression ceiling are narrowed/extended below.

Matrix V12 remains sole current machine source. Inventory V12 remains current closed-world companion.

## 1. Current manifest / policy resolution

Current normative manifest is resolved only through:

```text
PROJECT_GOVERNANCE/AHFMES_ARE_0_CURRENT_NORMATIVE_MANIFEST_BINDING.md
```

The binding must resolve Manifest V16 in the same exact subject.

Current quarantine policy is Policy V5. Policy V5 itself has no literal manifest-generation selector: it obtains `M(S)` only through the stable binding. Any older Policy V4 literal Manifest-V11/38-member routing is historical and replaced for current qualification.

Required theorem:

```text
Protocol current manifest selector
= Policy current manifest selector
= stable binding selector
= exactly one M(S)
```

No historical manifest, PR, index, handoff, QAO record or older Policy literal may substitute another member set.

## 2. Historical impact subject / blocker

Exact historical internal impact subject:

```text
7937744f196585651ec98ec86f07df374395d795
```

Machine Lane A-G disposition on that subject: no reproduced machine/scientific/capital blocker.

Lane H / SA-11 disposition:

```text
CHANGES_REQUIRED
IA16-H01 = WHOLE_BLOB_POLICY_STALE_MANIFEST_BINDING
ROOT = R9-03
NEW R9 ROOT = NO
```

Cause: current Policy V4 hard-coded Manifest V11/38 members while stable binding/current manifest had advanced to Manifest V15/54 members.

## 3. Stale-policy-routing attack charter

Before CP1 attack all current closure/audit-rule sources for:

```text
literal superseded manifest path
a hard-coded historical member count used as current N0
policy/protocol choosing different manifests
binding selects M but policy independently selects M_old
historical manifest self-promotes through version/status prose
```

Expected:

```text
current Policy V5 + Protocol V17 use stable binding only
historical literals may exist only in explicitly replaced immutable-base prose
UNKNOWN/conflicting selector -> qualification FAIL
```

## 4. Subject-bound SA-11 theorem

For exact frozen pre-pass subject `S0`:

```text
M0 = M(S0) from stable binding
N0 = exact member path set of M0
G0 = every recursive PROJECT_GOVERNANCE Git blob at S0
U0 = G0 - N0
```

SA-11 requires:

```text
SUBJECT_BOUND_CURRENT_NORMATIVE_MANIFEST_VALID
POLICY_PROTOCOL_MANIFEST_SELECTOR_IDENTITY_VALID
EXHAUSTIVE_G0_FRONTIER_PROVEN
WHOLE_BLOB_QUARANTINE(U0) universally applied
NO_UNLISTED_CURRENT_SEMANTIC_DEPENDENCY
EXACT_QAO_PATH_SET_FROZEN
NORMATIVE_CANDIDATE_TREE_ROOT computed from exact N0 path/blob/byte tuples
```

No remembered historical count/root may satisfy these predicates.

## 5. Independent audit lanes

Retain V16 lanes and make closure routing explicit:

```text
LANE-A instance / target authorization / sealed slot
LANE-B gen0 static-final partition / reconciliation drainability / materiality
LANE-C crash / coverage opportunity / source-currentness / CAS / terminality
LANE-D object/writer/control SoD / import-vs-coverage independence
LANE-E source contract / legacy-scientific-evidence-debt completeness / UNKNOWN
LANE-F Challenge / revalidation / rollback
LANE-G Safety / capital / broker / mutation-boundary
LANE-H manifest / binding / quarantine / candidate construction / historical authority / outside-family composition
```

No lane inherits another PASS. One reproducible legal exploit/deadlock/closure-qualification defect blocks CP1.

## 6. Permanent regression extension

All inherited R7/R8 and R9-X01..R9-X162 remain mandatory.

Add:

```text
R9-X163 current quarantine policy hard-codes superseded manifest generation/member count -> SA-11 denied
R9-X164 Policy and Protocol resolve different current manifests for same exact subject -> qualification denied
R9-X165 stable binding selects M16 but historical Policy V4 literal M11 is used to compute N0/U0 -> denied; Policy V5 resolver controls
R9-X166 post-CP1 binding/manifest membership changes while old S0/root/clean credit retained -> all clean credit resets; old lineage invalid
```

Current explicit R9 ceiling:

```text
R9-X01..R9-X166
```

## 7. Qualification reset / sequence

Policy V5 / Protocol V17 / binding / Correction V16 / Manifest V16 are normative changes before CP1.

```text
CLEAN PASS COUNT = 0
ALL PRIOR ROOT/PASS/CANDIDATE CREDIT = HISTORICAL ONLY
```

Required sequence:

```text
freeze exact corrected normative subject S0
-> Lane A-H whole-composition impact attack
-> SA-11 subject-bound whole-blob qualification via Policy V5
-> compute exact normative root from M(S0)
-> CP1
-> NO normative write
-> CP2 same root
-> R7 + R8 + R9-X01..X166 regression
-> final consistency + S0->candidate exact QAO-only proof
-> self-reference-free candidate
-> exactly one binder-only child
-> independent external whole-architecture re-audit
```

## 8. Progress / future chat requirement

Every completed external audit/re-audit must be written to GitHub progress/orientation metadata with exact subject, auditor, disposition, findings/root normalization, qualification reset/survival, firewall state and next activity.

Future Human–ARE conversational interface requirement remains preserved: chat captures structured intent/explanation/status/research interaction but cannot bypass THINK->PROVE->ACT, Safety, scientific, capital, mutation-boundary, broker-reconciliation or execution authority.

## 9. Static firewall

```text
ARE-0 CLOSED = NO
IMPLEMENTATION = NOT AUTHORIZED
P001 = NOT AUTHORIZED
PRODUCTION = CLOSED
LIVE/PAPER TRADING = NOT AUTHORIZED
PR #20 MERGE = NOT AUTHORIZED
```
