# AHFMES ARE — Self-Audit Council Protocol V3

Status: **MANDATORY PRE-EXTERNAL-AUDIT GOVERNANCE / BIG-WAVE WHOLE-TREE DISCIPLINE / STATELESS NORMATIVE PROTOCOL / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-21**

## 0. Composition

This protocol content-addresses the complete Wave-3 Council Protocol V2 as immutable base:

```text
BASE_PROTOCOL_V2_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_SELF_AUDIT_COUNCIL_PROTOCOL_V2.md
BASE_PROTOCOL_V2_GIT_BLOB_SHA = 0e90018eeae4ad5f24d76930fad70fbb5fdaf889
```

The exact V2 protocol remains in force except:

```text
V2 §6 normative authority path-set reference -> replaced by Normative Authority Manifest V2
V2 regression ceiling X44 -> extended by this V3
V2 attack overlays -> extended by Wave-3 residual attacks below
```

The 12-role council, concrete exploit format, two consecutive clean passes, stateless normative files, exact byte-root serialization, one-binder-only child and external whole-architecture audit requirements remain unchanged.

## 1. Hard sequencing remains large integrated design, then audit

```text
NORMALIZE ALL FINDINGS
        ↓
ONE COHERENT BIG SEMANTIC CORRECTION WAVE
        ↓
SYNCHRONIZE ALL NORMATIVE AUTHORITY BYTES
        ↓
WHOLE-COMPOSITION IMPACT ATTACK ON EXACT BYTES
        ↓
IF ANY REPRODUCIBLE BLOCKER:
  CLEAN COUNT REMAINS 0
  NO CLEAN PASS
  NORMALIZE ALL FINDINGS AGAIN
  NEXT WRITE IS ANOTHER BIG INTEGRATED WAVE

ONLY IF IMPACT-CLEAN:
  FREEZE EXACT NORMATIVE_CANDIDATE_TREE_ROOT R
  -> FULL COUNCIL CLEAN PASS #1
  -> NO NORMATIVE WRITE
  -> FULL COUNCIL CLEAN PASS #2 ON SAME R
  -> R7+R8+R9 REGRESSION ON SAME R
  -> CROSS-DOCUMENT CONSISTENCY ON SAME R
  -> FREEZE SELF-REFERENCE-FREE CANDIDATE
  -> ONE BINDER-ONLY CHILD
  -> EXTERNAL WHOLE-ARCHITECTURE AUDIT
```

Micro-patch convergence is not closure evidence.

## 2. Current normative authority manifest

The complete current normative path set is declared only by:

```text
PROJECT_GOVERNANCE/AHFMES_ARE_0_NORMATIVE_AUTHORITY_MANIFEST_V2.md
```

Manifest V1 is historical after V2 exists. `CURRENT_AUTHORITY_INDEX.md`, impact records, audit result records, PR/Issue metadata and binder/handoff records remain non-normative and cannot add machine rights.

## 3. Wave-4 mandatory attack overlays

### SA-01 / SA-05 — revalidation totality

Attack at minimum:

```text
two or more obligations born before first settlement
mutable prior disposition changing later opportunity identity
routine opportunity relabeled RECOVERY after evidence
SUSPENDED + routine PASS restoring CURRENT
REVOKED + outstanding PASS/FAIL/UNKNOWN/EXPIRED all needing terminal drain
new routine/recovery obligation generated after REVOKED
generic A-GOVERN bypassing specialized revalidation atomic group
CandidateProof REVALIDATION stuck at SCIENTIFIC_ADJUDICATED
deadline invalidation erasing sealed ScientificAdjudication result
proof episode left nonterminal while nonproof obligation settlement advances
RELIANCE_DEPENDENCY writer with missing or ambiguous state edge
```

### SA-06 — revalidation identity / order

Attack:

```text
same semantic trigger under changed current lineage
same trigger under retry/time/session/process wrapper
two triggers tied at same information time
a later obligation leapfrogging an earlier unsettled one
birth-time opportunity class changed after result access
REVALIDATION_PRIOR_OBLIGATION_SET_ROOT used as mutable key material
```

### SA-11 — completeness durable resolution

Attack:

```text
successor PASS laundering old adverse gap
two competing evidence packages minting two resolution slots
resolution record without exact adverse-gap identity
resolution without independent Audit/common-control separation
resolution without affected reliance invalidation/re-adjudication
historical FAIL/UNKNOWN rewritten or disappeared from adverse lineage
resolved gap becoming relied again after its resolution premise invalidates
DecisionInput resolution substituted for broker/exposure/Safety resolution
```

### SA-10 / SA-12 — composition

Attack:

```text
revalidation REVOKED while live exposure drains under R9-06
revalidation resolution/rollback target using stale R9-07 completeness lineage
completeness resolution changing R9-06 mutation-boundary admissibility
postaccess contaminated evidence reused in RECOVERY revalidation
```

## 4. Permanent regression extensions

All V2 R7/R8 and R9-X01..R9-X44 scenarios remain mandatory.

Wave-4 adds:

```text
R9-X45 O1 NEGATIVE revokes Champion while O2 already born; O2 PASS -> terminal record + REVOKED->REVOKED; queue advances
R9-X46 same as X45 but O2 UNKNOWN/EXPIRED/UNKNOWN_NO_PROOF -> terminal record + REVOKED->REVOKED
R9-X47 same as X45 but O2 FAIL/NEGATIVE -> terminal record + REVOKED->REVOKED
R9-X48 O2 key formed before O1 settles; O1 disposition changes; O2 key remains byte-identical
R9-X49 SUSPENDED Champion receives ordinary ROUTINE PASS -> remains SUSPENDED; no recovery privilege
R9-X50 SUSPENDED Champion receives pre-result mechanically classified RECOVERY PASS -> CURRENT only if frozen recovery rule/admissibility pass
R9-X51 CandidateProof[REVALIDATION] SCIENTIFIC_ADJUDICATED -> only specialized A-GOVERN atomic proof-edge + record + reliance CAS; generic A-GOVERN denied
R9-X52 deadline occurs with REVALIDATION proof pre-adjudication -> Audit terminalizes exact proof/reservation before nonproof settlement; queue drains
R9-X53 dependency invalidation while CURRENT/SUSPENDED -> REVOKED; later distinct invalidation while REVOKED -> sticky REVOKED revision
R9-X54 adverse completeness gap G has two competing evidence packages -> one COMPLETENESS_RESOLUTION_KEY; conflict becomes IntegrityDefect, not second resolution
R9-X55 valid exact backfill/dependency-removal resolution -> historical FAIL remains in adverse root; unresolved projection clears only exact gap/lineage
R9-X56 resolution premise/re-adjudication later invalidated -> gap re-enters unresolved projection without rewriting historical resolution record
```

No scenario is deleted merely because a later wave passes.

## 5. Exact normative-root discipline

The byte-exact V2 serialization grammar remains unchanged. Root membership comes from Normative Authority Manifest V2.

After Clean Pass #1:

```text
ANY byte/path/blob change in the manifest-declared normative set
= clean-pass sequence reset to 0
```

No editorial exception. Audit-progress state remains outside the normative root.

## 6. Clean-pass authorization rule

Impact attack against the exact integrated correction must return:

```text
NO NEW REPRODUCIBLE BLOCKER FOUND
```

before Clean Pass #1 is authorized.

A correction claim, auditor consensus, PR body or regression intent does not substitute for exact-byte attack.

## 7. Static boundary

This protocol grants no ARE-0 closure, implementation, P001 substantive research, production or PR-merge authority.