# AHFMES ARE — Self-Audit Council Protocol V10

Status: **MANDATORY PRE-EXTERNAL-REAUDIT GOVERNANCE / SUBJECT-BOUND MANIFEST + EXHAUSTIVE SA-11 / STATELESS / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-21**

## 0. Composition

Immutable protocol base:

```text
BASE_PROTOCOL_V9_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_SELF_AUDIT_COUNCIL_PROTOCOL_V9.md
BASE_PROTOCOL_V9_GIT_BLOB_SHA = 4cf914518c56112c72f881a24b40812d06941dfd
```

All V9->V2 protocol rules remain except V9 §1 literal manifest routing, SA-11 evidence mechanics, regression ceiling and dispatch sequence are replaced/narrowed here.

The 12-role council, isolated lanes, exact whole-tree/root discipline, two clean passes, permanent regression, final consistency, self-reference-free candidate and exactly one binder-only child remain mandatory.

## 1. Subject-bound current manifest

The current normative manifest is resolved only through:

```text
PROJECT_GOVERNANCE/AHFMES_ARE_0_CURRENT_NORMATIVE_MANIFEST_BINDING.md
```

For exact subject/tree `S`:

```text
M = SUBJECT_BOUND_CURRENT_NORMATIVE_MANIFEST(S)
```

Qualification is invalid unless the binding, referenced manifest, exact manifest membership/blob constraints and this Protocol are mutually consistent inside the same subject.

No historical manifest, PR body, Current Authority Index, handoff, audit record or higher-looking unlisted filename may override the binding.

Matrix V8 remains the sole current machine-semantic source unless a later integrated normative generation explicitly replaces it before Clean Pass #1.

## 2. R9-01 attacks retained

V9 §§2-4 remain mandatory without narrowing:

```text
semantic instance identity is independent of authorization/payload
same semantic system/control rotation cannot remint bootstrap instance
full exact generation-#0 authority-bearing content is committed
pre-genesis governed-knowledge closure is COMPLETE or conservative UNKNOWN
unbound import evidence cannot create genesis/scientific privilege
```

R9-X89..X93 remain permanent.

## 3. SA-11 exact-subject evidence theorem

SA-11 PASS requires all of the following against one exact subject/tree:

```text
SUBJECT_BOUND_CURRENT_NORMATIVE_MANIFEST_VALID
EXHAUSTIVE_UNLISTED_FRONTIER_PROVEN
AUTHORITY_CLAIM_TRIGGER_LEDGER_COMPLETE
AUTHORITY_LIKE_CLAIM_INVENTORY_COMPLETE
NO_UNLISTED_CURRENT_SEMANTIC_DEPENDENCY
SA11_EVIDENCE_FRESH_FOR_EXACT_SUBJECT
```

The current Legacy Authority Quarantine Policy controls detector and record mechanics.

Blanket set-difference classification is insufficient.

Every required trigger hit must be locatable in the trigger ledger. Every authority-like or UNKNOWN hit must map to a quarantine-record entry with exact path/blob/locator/claim class/classification.

Nested governance subtrees are inside the inspection frontier.

## 4. Stale-manifest-pointer attack rule

Audit must search all current normative closure/audit members for an operative literal pointer to a superseded manifest generation.

Expected:

```text
operative stale manifest pointer
=> SUBJECT_BOUND_CURRENT_NORMATIVE_MANIFEST_VALID = FALSE
=> SA-11 = FAIL
=> clean pass / final consistency / dispatch forbidden
```

Historical immutable-base prose may retain old literals only when the current higher-precedence member explicitly replaces that routing surface and no current dependency uses the old literal.

## 5. Stable binding integrity attacks

Required assertions:

```text
binding missing -> FAIL CLOSED
binding path altered -> FAIL CLOSED
binding references absent manifest -> FAIL CLOSED
referenced manifest omits binding -> FAIL CLOSED
referenced manifest binds wrong binding blob -> FAIL CLOSED
Policy/Protocol bypass binding with independent current-manifest selector -> FAIL CLOSED
unlisted higher-version manifest attempts self-promotion -> NO AUTHORITY
PR/index/handoff attempts manifest substitution -> NO AUTHORITY
```

The stable binding can select only a manifest; it cannot grant authority to an unlisted member.

## 6. Isolated audit lanes

Required lanes:

```text
LANE-A BOOTSTRAP IDENTITY / CRASH / CONCURRENCY
LANE-B CLOSED-WORLD STATE / WRITER / AUTHORITY TOTALITY
LANE-C SCIENTIFIC / CHALLENGE / REVALIDATION / ROLLBACK
LANE-D CAPITAL / BROKER / COMPLETENESS / SAFETY
LANE-E CLOSURE / MANIFEST / QUARANTINE / SUBJECT IDENTITY
LANE-F OUTSIDE-FAMILY ADVERSARIAL INTEGRATOR
```

No lane inherits another lane's PASS or finding disposition. One reproducible legal exploit is sufficient to block qualification.

## 7. Permanent regression extension

All inherited R7/R8 and R9-X01..R9-X93 remain mandatory.

Add:

```text
R9-X94 current quarantine policy/protocol retains operative literal pointer to superseded Manifest Vn -> SA-11/dispatch denied
R9-X95 stable manifest binding missing/ambiguous/mismatched with referenced manifest -> qualification denied
R9-X96 referenced current manifest omits stable binding or binds a different binding blob -> qualification denied
R9-X97 authority trigger hit exists in an unlisted governance blob but is absent from trigger ledger -> SA-11 denied
R9-X98 authority-like claim exists in nested PROJECT_GOVERNANCE subtree but is absent from quarantine inventory -> SA-11 denied
R9-X99 inspected governance blob/claim changes after evidence binding and evidence is not refreshed -> final consistency/dispatch denied
```

No prior regression seed is deleted.

## 8. Clean-pass reset and write discipline

This closure correction changes normative Policy/Protocol/Manifest/binding/correction bytes before Clean Pass #1:

```text
corrected normative-root clean-pass count = 0
```

After Clean Pass #1:

```text
ANY byte/path/blob change to any current normative manifest member
=> CLEAN PASS COUNT = 0
```

In particular, no Matrix status write is permitted after Clean Pass #1. Clean-pass results are recorded only in non-normative audit/council records.

## 9. Required dispatch sequence

```text
integrated pre-clean correction
-> freeze normative bytes for impact subject
-> isolated Lane A-F whole-composition impact attack
-> complete recursive SA-11 frontier + trigger ledger + per-claim quarantine evidence
-> resolve every impact/SA-11 blocker; any normative correction returns to start with count 0
-> freeze exact normative root
-> Clean Pass #1
-> NO normative write
-> Clean Pass #2 on exact same root
-> R7 + R8 + R9-X01..X99 regression
-> final cross-document consistency + fresh SA-11 verification
-> self-reference-free candidate
-> exactly one binder-only child
-> independent external whole-architecture audit
```

No internal pass is an external verdict.

## 10. Static boundary

This protocol grants no ARE-0 closure, implementation, P001 substantive research, production, live/paper trading or PR-merge authority.
