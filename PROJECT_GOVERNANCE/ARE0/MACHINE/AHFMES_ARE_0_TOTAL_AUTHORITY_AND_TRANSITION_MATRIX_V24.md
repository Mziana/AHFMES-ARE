# AHFMES ARE-0 — Canonical Authority & Transition Matrix V24

Status: **SOLE CURRENT MACHINE SOURCE / R9-01 REFINEMENT INFORMATION-FLOW + ISSUANCE NON-FORGEABILITY CLOSURE / STATELESS / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-22**

## 0. Composition / precedence

```text
BASE_MATRIX_V23_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_0_TOTAL_AUTHORITY_AND_TRANSITION_MATRIX_V23.md
BASE_MATRIX_V23_GIT_BLOB_SHA = 7c6496c8e359c0114b8101f42159ff6210d7c8fc
V24 R9-01 > EXACT V23 > EXACT V22 > ... > EXACT V1
```

V23's closed refinement-committer authority, factual-refinement/privilege-restoration split, conservative effective-gate union, crash/concurrency semantics and substantive admissibility remain fully in force. V24 narrows only release-driving information flow and refinement-VAR issuance/control topology.

## 1. Internal predecessor finding

Intermediate predecessor:

```text
57114ffd0148566d419ba7d3f58321359eb5afbd
```

was not granted S0 or qualification credit because impact audit found:

```text
IA29-H01
= REFINEMENT_CONSEQUENCE_BLINDNESS_LACKS_COMPLETE_INFORMATION_FLOW_NONINTERFERENCE
ROOT = R9-01
NEW ROOT = NO
```

A textual prohibition on outcome-conditioned relief is not sufficient unless every release-driving dependency is mechanically closed under provenance/dataflow ancestry. A renamed, transformed or mixed descendant of beneficiary outcome must not become an untainted historical-class feature.

## 2. Exact benefited-lineage closure

For each factual refinement subject `[D,S]`, derive non-writable:

```text
BENEFITED_AUTHORITY_LINEAGE_CLOSURE[D,S]
```

as the complete transitive set of governed objects/principals/control-equivalence aliases/genealogical descendants/dependency consumers whose scientific/search/evidence/selection/Champion/Promotion/Safety/broker/exposure/capital/deployment/execution eligibility, debt, gate, authority or risk permission would become strictly less conservative if the proposed authority-sensitive release were accepted.

The closure includes indirect beneficiaries, not only the immediate Research/Candidate named by a batch. Known common-control aliases are one beneficiary identity. Unknown material beneficiary ancestry/control equivalence => privilege restoration FALSE.

## 3. Beneficiary outcome-descendant closure

For `[D,S]`, derive non-writable:

```text
BENEFICIARY_OUTCOME_SOURCE_SET[D,S]
BENEFICIARY_OUTCOME_DESCENDANT_SET[D,S]
```

`BENEFICIARY_OUTCOME_SOURCE_SET` includes every governed result/access/utility/PnL/comparative outcome/Champion attractiveness/Promotion disposition/shadow or validation outcome/deployment performance/capital result, plus any human/operator/LLM observation event carrying such information, for any member of `BENEFITED_AUTHORITY_LINEAGE_CLOSURE`.

`BENEFICIARY_OUTCOME_DESCENDANT_SET` is the complete transitive content-derivation closure from those sources across:

```text
copy / rename / serialization / summarization
arithmetic / threshold / ranking / embedding / model inference
join / filter / aggregation / feature construction
human or LLM restatement / recommendation / label
cross-record propagation / cache / derived state
any other declared semantic data-dependency edge
```

A deterministic transform does not remove ancestry. A value with any path from an outcome source remains outcome-descended even if its name/schema/domain differs.

Mere chronological coexistence is not ancestry: an independently sourced market/public/external observation is not outcome-descended unless its content/provenance actually derives from or was selected/altered using beneficiary outcome information.

Incomplete/opaque/unknown material ancestry => treat as outcome-tainted for authority relief.

## 4. Complete release-driving dependency graph

For every `[D,S]`, derive immutable-for-the-batch:

```text
REFINEMENT_FACTUAL_DEPENDENCY_GRAPH[D,S]
REFINEMENT_PRIVILEGE_RELEASE_DEPENDENCY_GRAPH[D,S]
RELEASE_DRIVING_DEPENDENCY_CLOSURE_COMPLETE[D,S]
```

The privilege-release graph MUST enumerate every node and content-dependency edge capable of changing:

```text
historical class/scope used for relief
successor obligation union used for relief
REFINEMENT_AUTHORITY_SENSITIVE_RELEASE
REFINEMENT_PRIVILEGE_RESTORATION_ADMISSIBILITY_VALID
REFINEMENT_PRIVILEGE_RESTORATION_PROOF_ROOT
any predicate whose change can remove/weaken the conservative inherited gate
```

Every leaf binds exact:

```text
canonical evidence/claim/source identity
source/data-generating lineage
content provenance root
information/access time
historical referent D
frozen identification/admission method root
current/final-enough semantics
control-equivalence provenance where a governed principal can affect content/admission
```

No implicit configuration, implementation variable, prompt/context, hidden feature, model latent state, cached label, operator memory or undeclared external lookup may affect privilege release.

Any missing node/edge/leaf provenance, cycle whose source ancestry cannot be resolved, opaque model input ancestry, or material UNKNOWN => `RELEASE_DRIVING_DEPENDENCY_CLOSURE_COMPLETE = FALSE` and no privilege restoration.

## 5. Taint lattice / no washing

For each graph node `x`, derive:

```text
RELEASE_INPUT_TAINT[x] in {
  HISTORICAL_INDEPENDENT,
  OUTCOME_DESCENDED,
  MIXED,
  UNKNOWN
}
```

Rules:

```text
x in BENEFICIARY_OUTCOME_DESCENDANT_SET -> OUTCOME_DESCENDED

node with only positively proved HISTORICAL_INDEPENDENT parents
+ frozen historical-identification transform
+ no outcome access/control influence
-> HISTORICAL_INDEPENDENT

node with any OUTCOME_DESCENDED or MIXED parent
and any other parent
-> MIXED

unknown/incomplete ancestry -> UNKNOWN

rename/hash/encrypt/embed/summarize/model-transform/aggregate cannot downgrade taint
```

Only `HISTORICAL_INDEPENDENT` leaves/nodes may drive authority-sensitive privilege restoration. `OUTCOME_DESCENDED`, `MIXED`, or `UNKNOWN` may contribute to factual recording only under V23's conservative-union gate; they cannot reduce the inherited conservative gate.

## 6. Exact historical-identification requirement

A node may be `HISTORICAL_INDEPENDENT` only if it binds:

```text
HISTORICAL_REFERENT_ROOT = exact D / exact historical class-scope question
HISTORICAL_IDENTIFICATION_METHOD_ROOT
SOURCE_PROVENANCE_ROOT
OUTCOME_ISOLATION_ROOT
```

and positively proves that its content identifies the historical fact/class/scope under a frozen admissible relation.

Later discovery time is permitted. Post-outcome creation/discovery does not automatically invalidate factual information, but authority relief requires positive outcome isolation: the release-driving source/method/content selection must not observe, derive from, be chosen because of, or be modified by beneficiary outcome information.

If a post-outcome record was generated by a source that could observe the benefited outcome and such influence cannot be ruled out, it is not `HISTORICAL_INDEPENDENT` for privilege restoration.

## 7. Mechanical noninterference theorem

Derive:

```text
REFINEMENT_PRIVILEGE_RELEASE_NONINTERFERENCE_VALID[D,S]
REFINEMENT_PRIVILEGE_RELEASE_NONINTERFERENCE_ROOT[D,S]
```

TRUE only if all conditions hold:

```text
A. RELEASE_DRIVING_DEPENDENCY_CLOSURE_COMPLETE = TRUE;
B. every path into any authority-relief output is provenance-resolved;
C. there is NO directed data-dependency path from any
   BENEFICIARY_OUTCOME_SOURCE_SET / OUTCOME_DESCENDED / MIXED / UNKNOWN node
   to any privilege-restoration decision or authority-relief output;
D. every release-driving path terminates only in HISTORICAL_INDEPENDENT leaves
   with exact historical-identification and outcome-isolation proof;
E. holder/issuer/beneficiary SoD is valid;
F. removing all non-HISTORICAL_INDEPENDENT nodes leaves the exact same
   privilege-restoration decision and authority-relief result;
G. any material uncertainty about A-F => FALSE.
```

This is an information-flow property, not a vocabulary test. A frozen policy using `Z = f(favorable_outcome)` is invalid even if `Z` is named `historical_regime_feature` and even if `f` is deterministic/precommitted.

A frozen policy may still use outcome-tainted evidence in `REFINEMENT_FACTUAL_DEPENDENCY_GRAPH` to record factual hypotheses/results, but that tainted path cannot enter `REFINEMENT_PRIVILEGE_RELEASE_DEPENDENCY_GRAPH`; V23 conservative-union gating remains until independent release support exists.

## 8. Static projection rule admissibility strengthened

V23 `REFINEMENT_SEMANTIC_PROJECTION_ADMISSIBILITY_VALID` is TRUE only if the static rule language itself requires the graph/taint/noninterference proof above for every reachable authority-sensitive release.

The generation-0 static projection/admissibility spec MUST freeze:

```text
allowed dependency-edge grammar
required leaf provenance fields
historical-referent schema
outcome-source classification schema
transitive taint propagation rules
mixed/unknown treatment
noninterference decision algorithm
factual-only fallback semantics
```

The bootstrap/static author cannot mark a concrete dynamic input `HISTORICAL_INDEPENDENT`; that classification is derived from governed provenance under the frozen algorithm.

A static rule that omits the dependency graph or defines a taint downgrade is inadmissible before SystemGenesis.

## 9. Refinement VAR issuance is explicitly non-circular

Define exact proposed authority record identity:

```text
REFINEMENT_COMMIT_PROPOSED_VAR_ROOT[D,S,H]
= hash(
    authority_class,
    REFINEMENT_COMMIT_SUBJECT[D,S],
    exact holder H RoleManifest generation/control root,
    usage EDGE_NONCE,
    capital NO,
    exact prerequisite roots/generations,
    exact expiry/revocation profile
  )
```

Define passive target acceptance:

```text
REFINEMENT_COMMIT_TARGET_ACCEPTANCE_ROOT[D,S,H]
= signature/attestation by exact holder-control H over
  exact REFINEMENT_COMMIT_PROPOSED_VAR_ROOT[D,S,H]
```

Target acceptance grants **zero authority**. It cannot write `TrustedAuthorityRegistry`, cannot issue/revoke/modify a VAR, cannot select another holder/subject/prerequisite and is invalid if any proposed-VAR byte/root changes.

The sole writer that creates the VAR remains inherited:

```text
A-AUTHORITY-ISSUE
holder/executor = exact root gate
usage = ROOT
TrustedAuthorityRegistry exact-predecessor CAS
```

The authority being issued is not a prerequisite authority for its own issuance. Therefore no circular requirement exists.

## 10. Issuer / holder / beneficiary independence

Derive:

```text
REFINEMENT_VAR_ISSUER_HOLDER_SOD_VALID[D,S,H]
REFINEMENT_VAR_ISSUANCE_APPROVAL_ROOT[D,S,H]
```

TRUE only if:

```text
exact current root-kernel approval identity is known;
exact current root-gate writer control is known;
exact target AUDIT holder H control is known;
root-kernel/root-gate control-equivalence does not collapse into H;
H satisfies V23 release-driving/beneficiary independence;
root-kernel/root-gate control does not collapse into a materially benefited lineage
  where that control could discretionary approve the relief authority;
all RoleManifest/PrincipalRoleBinding generations current;
unknown material common control => FALSE.
```

`REFINEMENT_VAR_ISSUANCE_APPROVAL_ROOT` binds the proposed VAR, exact root-kernel approval, exact root-gate identity/generation, passive target acceptance, issuer-holder SoD root, release-driving SoD root, static projection/admissibility roots and currentness frontier.

A-AUTHORITY-ISSUE may create the refinement VAR only with this exact approval root. Generic root approval, generic Audit acceptance, operator/chat confirmation or implementation convention cannot substitute.

## 11. Effective release gate extension

V23 privilege restoration is narrowed to require all of:

```text
REFINEMENT_SEMANTIC_PROJECTION_ADMISSIBILITY_VALID
RELEASE_DRIVING_DEPENDENCY_CLOSURE_COMPLETE[D,S]
REFINEMENT_PRIVILEGE_RELEASE_NONINTERFERENCE_VALID[D,S]
REFINEMENT_COMMIT_PRINCIPAL_SOD_VALID[D,S]
REFINEMENT_VAR_ISSUER_HOLDER_SOD_VALID[D,S,H]
current/final-enough HISTORICAL_INDEPENDENT restoration support
exact current authority/VAR at commit
```

Any FALSE/UNKNOWN term => authority-sensitive inherited UNKNOWN remains in the V23 conservative union.

No factual batch, deterministic classification, static sealing, valid committer, or exact VAR can bypass this release gate.

## 12. Human–ARE / LLM dataflow

Any Human–ARE/LLM text, embedding, summary, tool output or derived feature that has observed a benefited outcome is part of `BENEFICIARY_OUTCOME_DESCENDANT_SET` if its content can flow into refinement. Prompt renaming or summarization cannot wash taint.

Chat may explain/simulate the graph and taint status but cannot classify a node as independent, sign target acceptance, issue VARs, admit release support, commit refinement, or waive conservative gating.

## 13. Static firewall

```text
ARE-0 CLOSED = NO
IMPLEMENTATION = NOT AUTHORIZED
P001 = NOT AUTHORIZED
PRODUCTION = CLOSED
LIVE/PAPER TRADING = NOT AUTHORIZED
PR #20 MERGE = NOT AUTHORIZED
```
