# AHFMES ARE-0 — Authority-Sensitive Object Inventory V22

Status: **CURRENT CLOSED-WORLD COMPANION / SEALED SEMANTIC-PROJECTION AUTHORITY / STATELESS / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-22**

## 0. Composition

```text
CURRENT_MACHINE = PROJECT_GOVERNANCE/AHFMES_ARE_0_TOTAL_AUTHORITY_AND_TRANSITION_MATRIX_V22.md
BASE_INVENTORY_V21_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_0_OBJECT_STATE_TOTALITY_REGISTER_V21.md
BASE_INVENTORY_V21_GIT_BLOB_SHA = fa382c97aa3db418481e49f6126d440e0e0ede23
```

## 1. POSTGENESIS_REFINEMENT_SEMANTIC_PROJECTION_ROOT

Exact `STATIC_PRECOMMITTED` generation-0 authority object. Immutable for one bootstrap journal lineage after authorization/import.

It binds semantic claim schema, normalization, equivalence, relevance, non-projectable adverse/currentness/causal qualifiers, support admission/aggregation, canonical encoding, and conservative UNKNOWN rules.

Writer: none after seal. Pre-seal value is part of static generation-0 authority payload and target-scoped authorization through inherited V9 machinery.

## 2. Projection totality predicate

Derived/non-writable:

```text
REFINEMENT_SEMANTIC_PROJECTION_TOTALITY_VALID
```

TRUE only if every reachable governed evidence type has exactly one deterministic projection/admission rule or explicit conservative UNKNOWN mapping, with no overlap ambiguity.

FALSE => no refinement privilege.

## 3. CURRENT_REFINEMENT_SEMANTIC_FRONTIER[D]

Derived only through the exact sealed projection root plus exact sealed classifier/source/materiality/causal contracts.

Companion root:

```text
CURRENT_REFINEMENT_SEMANTIC_ROOT[D] = hash(
  POST_CUT_OBLIGATION_CLASSIFICATION_ROOT,
  POSTGENESIS_REFINEMENT_SEMANTIC_PROJECTION_ROOT,
  canonical semantic frontier
)
```

No actor may write relevance/equivalence.

## 4. CURRENT_REFINEMENT_SUPPORT_SET[D]

Renewable support evidence remains non-authority identity. Per-claim support validity/currentness/finality is derived using the sealed projection root.

Unsupported/ambiguous raw evidence => conservative UNKNOWN, not implementation-defined behavior.

## 5. Refinement batch

`POSTGENESIS_CLASSIFICATION_REFINEMENT_BATCH[D,S]` key binds the exact projection root and semantic root. Same semantics under same sealed projection root can reuse the same batch through renewable support churn.

Projection-root mutation after seal cannot create a successor batch; it is static conflict.

## 6. Closed-world invariants

```text
PROJECTION POLICY = STATIC_PRECOMMITTED AUTHORITY
PROJECTION ROOT INCLUDED IN STATIC GEN0 COMMITMENT/AUTHORIZATION
NO IMPLEMENTATION-TIME EQUIVALENCE INVENTION
UNSUPPORTED EVIDENCE -> UNKNOWN
DIFFERENT PROJECTION ROOT != SAME SEMANTIC AUTHORITY IDENTITY
ADVERSE/CURRENTNESS/CAUSAL QUALIFIERS CANNOT BE PROJECTED AWAY
CHAT != PROJECTION AUTHORITY
```

All V21 support-loss, semantic-change, history, supersession and <=cut invariants remain.

## 7. Static firewall

```text
ARE-0 CLOSED = NO
IMPLEMENTATION = NOT AUTHORIZED
P001 = NOT AUTHORIZED
PRODUCTION = CLOSED
LIVE/PAPER TRADING = NOT AUTHORIZED
PR #20 MERGE = NOT AUTHORIZED
```
