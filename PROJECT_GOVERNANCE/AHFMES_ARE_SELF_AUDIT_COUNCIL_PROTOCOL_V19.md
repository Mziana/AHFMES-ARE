# AHFMES ARE — Self-Audit Council Protocol V19

Status: **MANDATORY PRE-EXTERNAL-REAUDIT GOVERNANCE / R9-01 POST-CUT HANDOFF + COMMIT-EVIDENCE RENEWAL ATTACK / STATELESS / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-22**

## 0. Composition

Immutable base:

```text
BASE_PROTOCOL_V18_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_SELF_AUDIT_COUNCIL_PROTOCOL_V18.md
BASE_PROTOCOL_V18_GIT_BLOB_SHA = d72cd3e61d840a629882cee23f3cbb713aa08f3a
```

All V18->V2 rules remain except current machine/inventory/manifest generation, external disposition and regression ceiling are advanced below.

Current machine = Matrix V14. Current closed-world companion = Inventory V14. Policy V5 remains current quarantine policy and resolves membership only through the stable binding.

## 1. Historical external adjudication

Exact immutable candidate:

```text
63ca962729facb6aaed322a97689fb890b6dac66
```

Project disposition:

```text
CHANGES_REQUIRED
ARE0_FORMALIZATION_INVALID = NO
NEW_R9_ROOT = NO
```

Normalized R9-01 findings:

```text
EA-LOCAL: POST_CUT_PRECOMMIT_FACT_HAS_NO_DURABLE_HANDOFF
EA2: LOCAL_CAS_POSTCUT_HEAD_ADVANCE_HAS_NO_TOTAL_RECOVERY
EA1: EXTERNAL_FINALITY_PROOF_RENEWAL_ONE_SLOT_DEADLOCK
```

Historical internal 240/240 remains evidence only and carries no clean-pass credit.

## 2. Integrated correction theorem

Audit current design as four distinct identities:

```text
SEMANTIC_FINALIZED_CUT_IDENTITY
SCIENTIFIC_COVERAGE_OPPORTUNITY
RENEWABLE_COMMIT_EVIDENCE_GENERATION
POST_CUT_PRECOMMIT_OBLIGATION_HANDOFF
```

No proof/fence refresh may become scientific novelty. No post-cut fact known before Genesis may disappear merely because it is outside the frozen semantic cut.

## 3. Independent lanes

```text
LANE-A instance / authorization / semantic-cut anti-selection
LANE-B LOCAL_CAS cut-fence refresh / starvation / crash / retry
LANE-C EXTERNAL_FINALIZABLE proof renewal / one-slot successor / concurrency
LANE-D post-cut precommit obligation object/writer/transition totality
LANE-E legacy/search/evidence/exposure/debt completeness + UNKNOWN propagation
LANE-F Challenge/revalidation/rollback adverse-history resistance
LANE-G Safety/capital/broker/new-risk privilege composition
LANE-H manifest/binding/quarantine/candidate chronology/outside-family composition
```

No lane inherits another PASS. One reproducible bypass/deadlock blocks CP1.

## 4. Mandatory attack questions

Before CP1 independently establish:

```text
1. harmless >cut LOCAL_CAS append cannot stale scientific coverage forever
2. repeated harmless local tail growth cannot remint Q or starve Genesis
3. proof expiry with unchanged semantic cut has exactly one legal renewal successor
4. competing proof renewals cannot produce proof lottery
5. renewal cannot select older/favorable semantic cut
6. <=cut correction cannot use mechanical refresh-only path
7. governed-known material >cut fact before Genesis cannot be omitted from queue
8. unknowable external post-cut tail creates conservative UNKNOWN obligation
9. Genesis atomically creates queue with generation0 and bootstrap consumption
10. pending/UNKNOWN queue cannot enable clean-history/new-risk privilege
11. queue has exact terminal post-genesis writer and serial-CAS semantics
12. refresh principal cannot acquire importer/Genesis/scientific/Safety/capital authority by composition
```

## 5. Permanent regression extension

All R7 26, R8 40 and inherited R9-X01..R9-X174 remain mandatory.

Add exactly:

```text
R9-X175 LOCAL_CAS H0 with semantic cut C and current Q/A; strictly >C append H0->H1 before Genesis -> no stale-H0 commit; same Q/A; deterministic cut-fence refresh or immutable-prefix proof; Genesis remains drainable

R9-X176 repeated harmless LOCAL_CAS >cut appends H0->H1->H2... -> no Q1/Q2 coverage lottery; no starvation solely from tail growth; <=cut mutation still invalidates semantic coverage

R9-X177 EXTERNAL_FINALIZABLE P0 valid for semantic cut C then P0 expires with facts unchanged; fresh P1 proves same C -> exactly one legal evidence successor generation; same Q/A; Genesis remains drainable

R9-X178 two concurrent fresh proof packages P1/P2 for same expired predecessor and same C -> one deterministic successor slot; conflicting payload cannot create two current generations or proof lottery

R9-X179 proof renewal attempts to substitute older/favorable/different semantic cut C0 != C -> denied; renewal cannot alter semantic cut

R9-X180 actual <=cut factual correction/reorg occurs while evidence refresh is attempted -> refresh-only path denied; semantic coverage/currentness invalid; normal reconciliation/fresh coverage required

R9-X181 material D with semantic order >C becomes governed-knowable before Genesis; operator delays commit -> D must appear in POST_CUT_PRECOMMIT_OBLIGATION_SET_ROOT and be atomically seeded into Generation0PostCutCorrectionQueue; clean lineage without D denied

R9-X182 external post-cut tail completeness cannot be positively proven -> UNKNOWN_POST_CUT_TAIL_OBLIGATION atomically seeded; no clean-history/new-risk privilege where positive completeness is required

R9-X183 crash/concurrency around SystemGenesis queue seed -> no partial generation0/queue/slot state; successful commit creates exactly one queue and consumes bootstrap atomically; retry idempotent

R9-X184 post-genesis queue obligation remains PENDING/UNKNOWN or conflicting terminal reconciliation is attempted -> clean privilege denied; only A-LEGACY-RECONCILE[POST_CUT_PRECOMMIT] serial-CAS terminal resolution is legal; no Research/Safety/Execution bypass
```

Current totals:

```text
R7 = 26
R8 = 40
R9 = 184
TOTAL = 250 explicit formal architecture scenarios
```

These are formal architecture scenario adjudications, not executable implementation-test claims.

## 6. Cross-root composition attacks

In addition to direct regressions, attack combinations:

```text
proof renewal + post-cut adverse evidence
LOCAL_CAS tail append + operator commit delay
UNKNOWN tail + comparator/incumbent cleanliness claim
pending queue + promotion/revalidation
pending queue + Safety contract bootstrap/migration
pending queue + capital new-risk reservation
legacy reconciliation + broker/runtime uncertainty
mechanical refresh principal + common-control role rotation
```

Expected rule: mechanical refresh never widens scientific/Safety/capital authority; pending/UNKNOWN history remains conservative.

## 7. Qualification reset and sequence

Matrix V14 / Inventory V14 / Protocol V19 / Correction V18 / stable binding / Manifest V18 are normative changes before CP1.

```text
CLEAN_PASS_COUNT = 0
ALL PRIOR ROOT/PASS/CANDIDATE CREDIT = HISTORICAL ONLY
```

Required sequence:

```text
freeze exact corrected normative subject S0
-> independent Lane A-H whole-composition impact attack
-> subject-bound SA-11 whole-blob quarantine via Policy V5
-> independently compute exact normative root twice
-> CP1
-> NO normative write
-> CP2 same root
-> formal R7 + R8 + R9-X01..X184 regression = 250/250 required
-> final consistency + exact S0->candidate QAO-only proof
-> self-reference-free candidate
-> exactly one binder-only child
-> independent external whole-architecture re-audit
```

Any normative byte change after CP1 resets all clean credit.

## 8. Candidate immutability / external discipline

Historical externally audited candidate `63ca962...` remains immutable `CHANGES_REQUIRED`. It must not be patched or relabeled as accepted.

Future auditor must audit the exact new immutable candidate, not its binder and not moving branch head.

## 9. Future Human–ARE interface requirement

Preserve the future conversational Human–ARE interface for explanation, status, research requests, hypotheses, simulation, audit inspection and governed intent capture.

Chat has zero ambient broker/capital authority and cannot bypass THINK->PROVE->ACT, scientific/Safety/selection gates, post-cut reconciliation, mutation-boundary reconciliation or execution authorization.

## 10. Static firewall

```text
ARE-0 CLOSED = NO
IMPLEMENTATION = NOT AUTHORIZED
P001 = NOT AUTHORIZED
PRODUCTION = CLOSED
LIVE/PAPER TRADING = NOT AUTHORIZED
PR #20 MERGE = NOT AUTHORIZED
```
