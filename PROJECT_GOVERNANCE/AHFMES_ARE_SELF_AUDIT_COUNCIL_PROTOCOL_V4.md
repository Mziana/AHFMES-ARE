# AHFMES ARE — Self-Audit Council Protocol V4

Status: **MANDATORY PRE-EXTERNAL-AUDIT GOVERNANCE / BIG-WAVE WHOLE-TREE DISCIPLINE / WAVE-5 EXTENSIONS / STATELESS NORMATIVE PROTOCOL / NO IMPLEMENTATION AUTHORITY**  
Effective date: **2026-08-21**

## 0. Composition

Immutable Wave-4 protocol base:

```text
BASE_PROTOCOL_V3_PATH = PROJECT_GOVERNANCE/AHFMES_ARE_SELF_AUDIT_COUNCIL_PROTOCOL_V3.md
BASE_PROTOCOL_V3_GIT_BLOB_SHA = fb2cc6b4cd1ccdffff748f63d0ad8b47910b2623
```

The exact V3 protocol remains fully in force except its current-manifest reference and regression ceiling are superseded below.

## 1. Current manifest

The complete current normative authority path set is declared only by:

```text
PROJECT_GOVERNANCE/AHFMES_ARE_0_NORMATIVE_AUTHORITY_MANIFEST_V3.md
```

Manifest V1/V2 are historical after V3 exists unless content-addressed as immutable bases by the current manifest.

## 2. Hard sequencing unchanged

```text
normalize all findings
-> one big integrated correction wave
-> synchronize all normative authority bytes
-> exact whole-composition impact attack
-> only if impact-clean freeze exact normative root
-> Clean Pass #1
-> no normative write
-> Clean Pass #2 on same root
-> R7+R8+R9 regression on same root
-> cross-document consistency
-> self-reference-free candidate
-> one binder-only child
-> external whole-architecture audit
```

Any reproducible blocker before Clean Pass #1 keeps clean count at 0. No micro-patch sequence is accepted as closure evidence.

## 3. Wave-5 mandatory attack overlays

### SA-01 / SA-11 — resolution generations

Attack:

```text
resolution g0 becomes invalid and same gap can never be re-resolved
resolution generation reuse/skip
parallel evidence payloads minting parallel same-generation resolution slots
successor resolution without canonical prior invalidation
successor resolution without materially new remediation
historical RESOLVED rewritten instead of append-only invalidation
resolution invalidation disappearing from effective unresolved projection
```

### SA-02 / SA-11 — resolution SoD

Attack:

```text
Audit principal creates discretionary backfill and self-attests it
same control plane operates reconstruction and resolution Audit under different process/host names
external/self-verifying source claimed without proof that audited producer cannot forge/suppress it
```

### SA-07 / SA-11 — gate preservation / completeness coupling

Attack:

```text
Matrix override accidentally deletes V2 §6.5 normal-new-risk gates
completeness resolution makes broker/exposure current but mutation-boundary generation remains stale/unrecomputed
resolution invalidation reopens gap while old mutation-boundary generation still authorizes new risk
```

## 4. Permanent regression extension

All earlier R7/R8 families and R9-X01..R9-X56 remain mandatory.

Wave-5 adds:

```text
R9-X57 completeness resolution g0 valid -> later premise invalidated -> gap unresolved -> material remediation -> g1 RESOLVED
R9-X58 two competing g1 evidence payloads -> one slot; one canonical payload, conflict becomes IntegrityDefect
R9-X59 successor resolution requested without canonical prior invalidation -> denied
R9-X60 resolution Audit common-controlled with discretionary backfill producer -> denied unless source positively external/self-verifying
R9-X61 Matrix V4 composition preserves every applicable V2 §6.5 normal-new-risk predicate
R9-X62 completeness resolution/invalidation head changes -> stale protected-scope mutation-boundary generation cannot authorize new risk until governed reconciliation/current boundary
```

No scenario is removed merely because a later wave passes.

## 5. Exact-root discipline

The exact V2/V3 length-prefixed root serialization grammar remains unchanged. Root membership comes from Normative Authority Manifest V3.

After Clean Pass #1 any byte/path/blob change in the manifest-declared set resets the clean-pass sequence to zero.

## 6. Impact-clean authorization

Clean Pass #1 requires an exact-byte integrated impact result of:

```text
NO NEW REPRODUCIBLE BLOCKER FOUND
```

Consensus, prose intent, PR metadata or per-root isolated correctness is insufficient.

## 7. Static boundary

This protocol grants no ARE-0 closure, implementation, P001 substantive research, production or PR-merge authority.